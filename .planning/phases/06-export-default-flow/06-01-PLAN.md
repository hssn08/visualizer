---
phase: 06-export-default-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/toolbar/ExportButton.tsx
  - src/components/toolbar/__tests__/ExportButton.test.tsx
  - src/components/preview/JsonPreviewPanel.tsx
  - src/components/preview/__tests__/JsonPreviewPanel.test.tsx
  - src/App.tsx
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04

must_haves:
  truths:
    - "Export button downloads a .json file named after flow_name (or flow.json fallback)"
    - "Exported JSON preserves all original fields not visually represented"
    - "Exported JSON connection fields reflect current edge state"
    - "JSON preview panel toggles open/closed via toolbar JSON button"
    - "Preview panel shows formatted current JSON state"
    - "Preview panel has a close button"
  artifacts:
    - path: "src/components/toolbar/ExportButton.tsx"
      provides: "Export with dynamic filename from flow_name"
      contains: "flow_name"
    - path: "src/components/toolbar/__tests__/ExportButton.test.tsx"
      provides: "Export button unit tests"
      min_lines: 30
    - path: "src/components/preview/JsonPreviewPanel.tsx"
      provides: "Live JSON preview panel component"
      exports: ["JsonPreviewPanel"]
      min_lines: 25
    - path: "src/components/preview/__tests__/JsonPreviewPanel.test.tsx"
      provides: "Preview panel unit tests"
      min_lines: 30
    - path: "src/App.tsx"
      provides: "Preview panel wired into layout"
      contains: "JsonPreviewPanel"
  key_links:
    - from: "src/components/toolbar/ExportButton.tsx"
      to: "src/lib/flowToJson.ts"
      via: "flowToJson call for export"
      pattern: "flowToJson\\("
    - from: "src/components/preview/JsonPreviewPanel.tsx"
      to: "src/lib/flowToJson.ts"
      via: "flowToJson call for live preview"
      pattern: "flowToJson\\("
    - from: "src/App.tsx"
      to: "src/components/preview/JsonPreviewPanel.tsx"
      via: "conditional render when jsonPreviewOpen"
      pattern: "jsonPreviewOpen.*JsonPreviewPanel"
---

<objective>
Enhance export with dynamic filenames and build the live JSON preview panel.

Purpose: Complete the export pipeline (EXP-01/02/03) and provide live preview (EXP-04) so users can see and download their current flow state as JSON at any time.
Output: Enhanced ExportButton, new JsonPreviewPanel component, both wired into App layout with tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-export-default-flow/06-RESEARCH.md

@src/components/toolbar/ExportButton.tsx
@src/components/toolbar/Toolbar.tsx
@src/store/types.ts
@src/store/uiSlice.ts
@src/store/index.ts
@src/lib/flowToJson.ts
@src/lib/types.ts
@src/App.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/store/types.ts:
```typescript
export interface FlowSlice {
  nodes: Node[];
  edges: Edge[];
  rawJson: Record<string, unknown> | null;
  metadata: JsonMetadata | null;
  // ... actions
}

export interface UiSlice {
  selectedNodeId: string | null;
  setSelectedNodeId: (id: string | null) => void;
  jsonPreviewOpen: boolean;
  toggleJsonPreview: () => void;
}

export type AppState = FlowSlice & UiSlice;
```

From src/lib/types.ts:
```typescript
export interface JsonMetadata {
  stepsKey: string;
  wrapperFields: Record<string, unknown>;
}
```

From src/lib/flowToJson.ts:
```typescript
export function flowToJson(
  nodes: Node[],
  edges: Edge[],
  metadata: JsonMetadata
): Record<string, unknown>;
```

From src/store/uiSlice.ts:
```typescript
// jsonPreviewOpen: boolean (already exists)
// toggleJsonPreview: () => void (already exists)
```

From src/components/toolbar/ExportButton.tsx (current):
```typescript
// Uses flowToJson -> Blob -> createObjectURL -> anchor click -> revokeObjectURL
// Currently hardcodes a.download = 'flow.json'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhance ExportButton with dynamic filename and add tests</name>
  <files>src/components/toolbar/ExportButton.tsx, src/components/toolbar/__tests__/ExportButton.test.tsx</files>
  <behavior>
    - Test 1: Export button renders and is disabled when metadata is null
    - Test 2: Export button is enabled when metadata exists (store has imported flow)
    - Test 3: handleExport creates Blob with correct JSON content (mock createObjectURL, verify Blob content type is application/json)
    - Test 4: handleExport uses sanitized flow_name from metadata.wrapperFields for filename (e.g., "Medicare Enrollment" -> "medicare_enrollment.json")
    - Test 5: handleExport falls back to "flow.json" when flow_name is absent
    - Test 6: handleExport calls URL.revokeObjectURL to prevent memory leak
  </behavior>
  <action>
    1. Write tests first in `src/components/toolbar/__tests__/ExportButton.test.tsx`:
       - Import render, screen, fireEvent from @testing-library/react
       - Mock URL.createObjectURL and URL.revokeObjectURL on globalThis
       - Mock document.createElement to capture anchor properties
       - Use useAppStore.setState to set up store state with/without metadata
       - Test disabled state, enabled state, filename derivation, Blob creation, URL cleanup
       - Use the sampleFlow fixture to populate store via importJson for realistic tests

    2. Run tests (should fail RED since filename logic doesn't exist yet).

    3. Enhance `src/components/toolbar/ExportButton.tsx`:
       - After `const json = flowToJson(nodes, edges, metadata);`, extract flow_name:
         `const flowName = metadata.wrapperFields.flow_name as string | undefined;`
       - Sanitize: `const safeName = (flowName ?? 'flow').replace(/[^a-zA-Z0-9_-]/g, '_').toLowerCase();`
       - Set `a.download = \`\${safeName}.json\`;` instead of hardcoded 'flow.json'

    4. Run tests (should pass GREEN).

    Note: EXP-02 and EXP-03 are already verified by existing flowToJson.test.ts (18 tests) and roundTrip.test.ts (6 tests). No additional implementation needed for those -- the ExportButton already calls flowToJson which handles field preservation and connection field updates.
  </action>
  <verify>
    <automated>npx vitest run src/components/toolbar/__tests__/ExportButton.test.tsx --reporter verbose</automated>
  </verify>
  <done>ExportButton uses flow_name for dynamic filename with sanitization and fallback. All 6+ tests pass. URL.revokeObjectURL is called. EXP-01 verified via tests, EXP-02/EXP-03 verified by existing flowToJson/roundTrip tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create JsonPreviewPanel component, wire into App, and add tests</name>
  <files>src/components/preview/JsonPreviewPanel.tsx, src/components/preview/__tests__/JsonPreviewPanel.test.tsx, src/App.tsx</files>
  <behavior>
    - Test 1: JsonPreviewPanel renders formatted JSON when metadata and nodes exist in store
    - Test 2: JsonPreviewPanel returns null when metadata is null
    - Test 3: JsonPreviewPanel shows a header with "JSON Preview" text and a close button
    - Test 4: Clicking close button calls toggleJsonPreview (toggles jsonPreviewOpen to false)
    - Test 5: Rendered JSON contains flow_name and steps from the imported flow
    - Test 6 (App): JsonPreviewPanel is NOT rendered when jsonPreviewOpen is false
    - Test 7 (App): JsonPreviewPanel IS rendered when jsonPreviewOpen is true and metadata exists
  </behavior>
  <action>
    1. Write tests first in `src/components/preview/__tests__/JsonPreviewPanel.test.tsx`:
       - Import render, screen, fireEvent from @testing-library/react
       - Use useAppStore.setState and useAppStore.getState().importJson(sampleFlow) for state setup
       - Test rendering, null guard, header text, close button, JSON content
       - For close button test: verify that clicking it toggles jsonPreviewOpen via store state check

    2. Add App-level tests in `src/__tests__/App.test.tsx` (append to existing file):
       - Test that JsonPreviewPanel does not render when jsonPreviewOpen is false
       - Test that JsonPreviewPanel renders when jsonPreviewOpen is true and metadata exists
       - Use data-testid="json-preview-panel" for panel identification

    3. Run tests (should fail RED).

    4. Create `src/components/preview/JsonPreviewPanel.tsx`:
       - Import useShallow from zustand/react/shallow
       - Import useAppStore from @/store
       - Import flowToJson from @/lib/flowToJson
       - Import Button from @/components/ui/button
       - Import ScrollArea from @/components/ui/scroll-area
       - Import X from lucide-react
       - Subscribe to nodes, edges, metadata, toggleJsonPreview via useShallow
       - Guard: if (!metadata) return null
       - Compute: `const json = flowToJson(nodes, edges, metadata)`
       - Compute: `const formatted = JSON.stringify(json, null, 2)`
       - Render: div with data-testid="json-preview-panel", className="w-96 border-l flex flex-col min-h-0"
       - Header: flex row with "JSON Preview" span + ghost X button calling toggleJsonPreview
       - Body: ScrollArea wrapping pre with className="p-3 text-xs font-mono whitespace-pre overflow-x-auto" containing {formatted}

    5. Wire into `src/App.tsx`:
       - Import JsonPreviewPanel from @/components/preview/JsonPreviewPanel
       - Add `const jsonPreviewOpen = useAppStore((s) => s.jsonPreviewOpen);` selector
       - Add `{jsonPreviewOpen && <JsonPreviewPanel />}` after the PropertyPanel conditional, inside the flex container
       - The layout becomes: NodePalette | FlowCanvas (flex-1) | PropertyPanel? | JsonPreviewPanel?

    6. Run tests (should pass GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/components/preview/__tests__/JsonPreviewPanel.test.tsx src/__tests__/App.test.tsx --reporter verbose</automated>
  </verify>
  <done>JsonPreviewPanel renders live formatted JSON from flowToJson, has close button wired to toggleJsonPreview, and is conditionally rendered in App.tsx based on jsonPreviewOpen state. All preview panel tests and App integration tests pass. EXP-04 verified.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx vitest run --reporter verbose` -- full test suite passes (219 existing + new tests)
2. ExportButton downloads a file named after flow_name (e.g., "medicare_enrollment.json")
3. JSON toolbar button toggles preview panel open/closed
4. Preview panel shows formatted JSON matching the current flow state
5. Closing preview panel via X button works
</verification>

<success_criteria>
- EXP-01: Export button triggers download of .json file with dynamic filename
- EXP-02: Exported JSON preserves all non-visual fields (verified by existing 18+ flowToJson tests)
- EXP-03: Connection fields reflect current edges (verified by existing roundTrip tests)
- EXP-04: JSON preview panel toggles open/closed showing formatted current state
- All new and existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-export-default-flow/06-01-SUMMARY.md`
</output>
