---
phase: 03-canvas-rendering-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/canvas/StepNode.tsx
  - src/components/canvas/nodeTypes.ts
  - src/lib/nodeClassify.ts
  - src/lib/jsonToFlow.ts
  - src/lib/__tests__/nodeClassify.test.ts
  - src/components/canvas/__tests__/StepNode.test.tsx
autonomous: true
requirements: [NODE-01, NODE-02, NODE-03, NODE-04, NODE-05, NODE-06, NODE-07, NAV-01, NAV-02]

must_haves:
  truths:
    - "Each node displays its step key as a header and description below it"
    - "Nodes have color-coded borders: green=start, red=terminal, orange=error, blue=normal"
    - "Info badges appear for key fields like wait_for_response, disposition, action, criticalstep"
    - "Each node has one input handle at the top"
    - "Each node has multiple labeled output handles (next, conditions, timeout, no_match, intents)"
    - "Selected node has a visible highlight ring"
    - "Imported JSON renders nodes using custom StepNode component (not default rectangles)"
  artifacts:
    - path: "src/components/canvas/StepNode.tsx"
      provides: "Custom node component with header, description, badges, handles"
      min_lines: 60
    - path: "src/components/canvas/nodeTypes.ts"
      provides: "Module-level nodeTypes registry mapping 'step' to StepNode"
      exports: ["nodeTypes"]
    - path: "src/lib/nodeClassify.ts"
      provides: "Node role classification and color mapping"
      exports: ["classifyNodeRole", "ROLE_COLORS", "NodeRole"]
    - path: "src/lib/jsonToFlow.ts"
      provides: "Updated stepsToNodes with type: 'step' instead of 'default'"
      contains: "type: 'step'"
  key_links:
    - from: "src/components/canvas/StepNode.tsx"
      to: "src/lib/nodeClassify.ts"
      via: "classifyNodeRole import"
      pattern: "import.*classifyNodeRole.*from.*nodeClassify"
    - from: "src/components/canvas/nodeTypes.ts"
      to: "src/components/canvas/StepNode.tsx"
      via: "nodeTypes registry"
      pattern: "step.*StepNode"
    - from: "src/lib/jsonToFlow.ts"
      to: "nodeTypes registry"
      via: "type: 'step' on every node"
      pattern: "type.*'step'"
---

<objective>
Create the custom StepNode component that renders call flow steps as color-coded cards with headers, descriptions, info badges, and dynamic connection handles. Update jsonToFlow to emit nodes with type: 'step' so React Flow uses the custom component.

Purpose: Replace default rectangles with rich, informative node cards that show step structure at a glance.
Output: StepNode.tsx, nodeTypes.ts, nodeClassify.ts, updated jsonToFlow.ts, unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-canvas-rendering-layout/03-RESEARCH.md

@src/lib/jsonToFlow.ts
@src/lib/edgeExtractors.ts
@src/lib/types.ts
@src/store/types.ts
@src/components/canvas/FlowCanvas.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/types.ts:
```typescript
export interface JsonMetadata {
  stepsKey: string;
  wrapperFields: Record<string, unknown>;
}
export interface FlowTransformResult {
  nodes: Node[];
  edges: Edge[];
  metadata: JsonMetadata;
}
```

From src/lib/jsonToFlow.ts (stepsToNodes - the function to modify):
```typescript
// Current: type: 'default' -- must change to type: 'step'
// Current: position computed by grid -- dagre in Plan 03 will replace
function stepsToNodes(steps: Record<string, Record<string, unknown>>): Node[] {
  const entries = Object.entries(steps);
  return entries.map(([key, step], i) => ({
    id: key,
    type: 'default' as const,  // CHANGE TO 'step'
    position: { x: (i % COLS) * X_GAP, y: Math.floor(i / COLS) * Y_GAP },
    data: {
      label: (step.description as string) || (step.name as string) || key,
      step: { ...step },
    },
  }));
}
```

From src/lib/edgeExtractors.ts (sourceHandle IDs that StepNode handles must match):
```typescript
// sourceHandle values used in edges:
// 'next' (string)
// 'condition-0', 'condition-1', ... (indexed)
// 'timeout' (string)
// 'no_match' (string)
// 'intent-DNC', 'intent-not_interested', ... (named)
```

Node data structure (every node has):
```typescript
node.id = stepKey (e.g., "greeting", "verify_identity")
node.data.label = step.description || step.name || key
node.data.step = { ...fullStepObject }  // all original fields preserved
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create nodeClassify and StepNode with tests</name>
  <files>
    src/lib/nodeClassify.ts,
    src/lib/__tests__/nodeClassify.test.ts,
    src/components/canvas/StepNode.tsx,
    src/components/canvas/__tests__/StepNode.test.tsx,
    src/components/canvas/nodeTypes.ts
  </files>
  <behavior>
    nodeClassify tests:
    - classifyNodeRole returns 'start' when isFirstNode=true
    - classifyNodeRole returns 'terminal' when step.action is 'hangup'
    - classifyNodeRole returns 'terminal' when step.action is 'transfer'
    - classifyNodeRole returns 'error' when description contains 'retry'
    - classifyNodeRole returns 'error' when description contains 'error'
    - classifyNodeRole returns 'normal' for a basic step with no special markers
    - ROLE_COLORS maps all 4 roles to { border, bg, minimap } color strings

    StepNode tests (using @testing-library/react, render StepNode directly with mock props):
    - Renders step key (node id) as header text
    - Renders step description text
    - Applies green border class when node is first (start)
    - Applies red border class when step.action='hangup' (terminal)
    - Applies ring/glow class when selected=true
    - Renders 'wait_for_response' badge when step has that field set to true
    - Renders 'action' badge when step has an action field
    - Renders a target handle with id='target'
    - Renders source handle with id='next' when step has a next field
    - Renders source handles for conditions (condition-0, condition-1)
    - Renders source handle with id='timeout' when step has timeout_next
    - Renders source handle with id='no_match' when step has no_match_next
    - Renders source handles for intent_detector_routes
    - Each source handle has a visible label
  </behavior>
  <action>
    1. Create src/lib/nodeClassify.ts:
       - Export type NodeRole = 'start' | 'terminal' | 'error' | 'normal'
       - Export function classifyNodeRole(nodeId: string, step: Record<string, unknown>, isFirstNode: boolean): NodeRole
         - If isFirstNode -> 'start'
         - If step.action === 'hangup' or 'transfer' -> 'terminal'
         - If step.description contains 'retry', 'error', or 'recovery' (case-insensitive) -> 'error'
         - Otherwise -> 'normal'
       - Export const ROLE_COLORS: Record<NodeRole, { border: string; bg: string; minimap: string }>
         - start: { border: 'border-green-500', bg: 'bg-green-50', minimap: '#22c55e' }
         - terminal: { border: 'border-red-500', bg: 'bg-red-50', minimap: '#ef4444' }
         - error: { border: 'border-orange-500', bg: 'bg-orange-50', minimap: '#f97316' }
         - normal: { border: 'border-blue-500', bg: 'bg-blue-50', minimap: '#3b82f6' }

    2. Create src/components/canvas/StepNode.tsx:
       - Import Handle, Position from @xyflow/react. Import classifyNodeRole, ROLE_COLORS from nodeClassify.
       - Accept NodeProps with data: { label: string; step: Record<string, unknown> }
       - The component needs an isFirstNode prop or context. Since React Flow custom nodes don't receive global context about sibling nodes, pass isFirstNode as node.data.isFirstNode (will be set in jsonToFlow in Task 2).
       - Render structure:
         - Outer div with: rounded-lg border-2 shadow-md p-3 min-w-[200px] max-w-[280px]
         - Apply ROLE_COLORS[role].border and ROLE_COLORS[role].bg classes
         - When selected: add ring-2 ring-blue-400 shadow-lg
         - Target Handle at Position.Top with id="target"
         - Header: node id in font-semibold text-sm truncate
         - Description: step.description in text-xs text-muted-foreground line-clamp-2
         - Badges row: flex flex-wrap gap-1 mt-1.5
           - If step.wait_for_response truthy: badge "wait_for_response"
           - If step.action: badge with action value
           - If step.disposition: badge "disposition"
           - If step.criticalstep: badge "critical"
         - Badge styling: text-[10px] px-1.5 py-0.5 rounded-full bg-slate-100 text-slate-600 font-medium
         - Source handles section below badges with small labels:
           - Build handles array using buildOutputHandles(step) helper function (defined in the same file)
           - buildOutputHandles returns { id: string; label: string }[] by checking step.next, step.conditions, step.timeout_next, step.no_match_next, step.intent_detector_routes
           - Handle IDs MUST match the sourceHandle IDs from edgeExtractors: 'next', 'condition-0', 'condition-1', 'timeout', 'no_match', 'intent-{name}'
           - Render each Handle at Position.Bottom with style={{ left: `${((i+1)/(handles.length+1))*100}%` }}
           - Below each handle, render a small label (text-[9px] text-muted-foreground) positioned absolutely

    3. Create src/components/canvas/nodeTypes.ts:
       - Module-level constant (NOT inside a component, per React Flow requirement)
       - Export const nodeTypes: NodeTypes = { step: StepNode }

    4. Note for StepNode testing: React Flow Handle components require ReactFlowProvider context. Wrap StepNode renders in a minimal ReactFlowProvider in tests. If Handle rendering is problematic in jsdom, test the handle data derivation (buildOutputHandles) as a separate exported function and test DOM structure for non-Handle elements.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/nodeClassify.test.ts src/components/canvas/__tests__/StepNode.test.tsx --reporter verbose</automated>
  </verify>
  <done>
    nodeClassify correctly maps step data to 4 roles with color objects.
    StepNode renders header, description, badges, target handle, and dynamic source handles.
    nodeTypes registry exports { step: StepNode } at module level.
    All new tests pass alongside existing 72 tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update jsonToFlow to emit type:'step' and set isFirstNode</name>
  <files>
    src/lib/jsonToFlow.ts,
    src/lib/__tests__/jsonToFlow.test.ts
  </files>
  <action>
    1. In src/lib/jsonToFlow.ts, update stepsToNodes:
       - Change `type: 'default'` to `type: 'step'` on every node
       - Add `isFirstNode: i === 0` to node.data so StepNode can determine start color
       - Keep the grid layout positions for now (dagre replaces them in Plan 03)

    2. Update existing jsonToFlow tests to expect type: 'step' instead of 'default'. The tests in src/lib/__tests__/jsonToFlow.test.ts likely check node.type -- update those assertions.

    3. Run the full test suite to ensure no regressions from the type change.
  </action>
  <verify>
    <automated>npx vitest run --reporter verbose</automated>
  </verify>
  <done>
    All nodes emitted by jsonToFlow have type: 'step' and data.isFirstNode set correctly.
    First node has isFirstNode=true, all others false.
    All 72+ existing tests still pass after type change.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter verbose` -- all tests pass (existing 72 + new nodeClassify + StepNode tests)
- nodeClassify.ts exports classifyNodeRole and ROLE_COLORS
- StepNode.tsx renders header, description, badges, handles from step data
- nodeTypes.ts exports module-level { step: StepNode }
- jsonToFlow.ts emits type: 'step' on all nodes
</verification>

<success_criteria>
- Nodes use custom StepNode when rendered by React Flow (type: 'step' maps to StepNode via nodeTypes)
- Step key appears as node header, description appears below
- Color coding: green border for first node, red for hangup/transfer, orange for error/retry, blue for normal
- Info badges render for wait_for_response, action, disposition, criticalstep
- One target handle at top, multiple labeled source handles at bottom
- Selected node gets visual highlight ring
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-canvas-rendering-layout/03-01-SUMMARY.md`
</output>
