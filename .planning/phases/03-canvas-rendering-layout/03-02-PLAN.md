---
phase: 03-canvas-rendering-layout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/canvas/ConditionalEdge.tsx
  - src/components/canvas/edgeTypes.ts
  - src/lib/edgeExtractors.ts
  - src/components/canvas/__tests__/ConditionalEdge.test.tsx
  - src/lib/__tests__/edgeExtractors.test.ts
autonomous: true
requirements: [EDGE-01, EDGE-02, EDGE-03, EDGE-04, EDGE-05, EDGE-06]

must_haves:
  truths:
    - "Normal 'next' edges render as solid slate lines"
    - "Condition edges render as solid blue lines with colored label badges"
    - "Timeout edges render as dashed orange lines"
    - "No-match/fallback edges render as dashed gray lines"
    - "Intent route edges render as dotted red lines"
    - "All edges with labels display their label text in a badge"
  artifacts:
    - path: "src/components/canvas/ConditionalEdge.tsx"
      provides: "Custom edge component with 5 visual style variants and label badges"
      min_lines: 40
    - path: "src/components/canvas/edgeTypes.ts"
      provides: "Module-level edgeTypes registry mapping 'conditional' to ConditionalEdge"
      exports: ["edgeTypes"]
    - path: "src/lib/edgeExtractors.ts"
      provides: "Updated edges with type: 'conditional' on every edge"
      contains: "type: 'conditional'"
  key_links:
    - from: "src/components/canvas/edgeTypes.ts"
      to: "src/components/canvas/ConditionalEdge.tsx"
      via: "edgeTypes registry"
      pattern: "conditional.*ConditionalEdge"
    - from: "src/lib/edgeExtractors.ts"
      to: "edgeTypes registry"
      via: "type: 'conditional' on every edge"
      pattern: "type.*'conditional'"
    - from: "src/components/canvas/ConditionalEdge.tsx"
      to: "edge.data.edgeType"
      via: "style lookup from EDGE_STYLES map"
      pattern: "EDGE_STYLES.*edgeType"
---

<objective>
Create the custom ConditionalEdge component that renders edges with 5 distinct visual styles (solid, dashed, dotted) based on connection type, plus label badges on all labeled edges. Update edgeExtractors to emit type: 'conditional' so React Flow uses the custom component.

Purpose: Make flow connections visually distinguishable -- users can tell at a glance which edges are normal flow, conditions, timeouts, fallbacks, or intent routes.
Output: ConditionalEdge.tsx, edgeTypes.ts, updated edgeExtractors.ts, unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-canvas-rendering-layout/03-RESEARCH.md

@src/lib/edgeExtractors.ts
@src/lib/types.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/edgeExtractors.ts (current edge structure):
```typescript
// Every edge has:
// - id: `${stepKey}->${type}->${target}`
// - source: stepKey
// - target: targetStepKey
// - sourceHandle: 'next' | 'condition-N' | 'timeout' | 'no_match' | 'intent-{name}'
// - data: { edgeType: 'next' | 'condition' | 'timeout' | 'no_match' | 'intent', ... }
// - label?: string (present on condition, timeout, no_match, intent edges)

// CURRENTLY MISSING: `type` field. Must add `type: 'conditional'` to all edges.
```

From @xyflow/react (edge component props):
```typescript
// EdgeProps includes: id, source, target, sourceX, sourceY, targetX, targetY,
//   sourcePosition, targetPosition, data, label, markerEnd, style, selected
// Use getSmoothStepPath for path computation
// Use BaseEdge for rendering the SVG path
// Use EdgeLabelRenderer for HTML labels positioned over edges
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ConditionalEdge component with style variants and label badges</name>
  <files>
    src/components/canvas/ConditionalEdge.tsx,
    src/components/canvas/__tests__/ConditionalEdge.test.tsx,
    src/components/canvas/edgeTypes.ts
  </files>
  <behavior>
    ConditionalEdge tests (test the EDGE_STYLES map and label rendering logic):
    - EDGE_STYLES['next'] has solid stroke (no strokeDasharray), slate color '#64748b'
    - EDGE_STYLES['condition'] has solid stroke (no strokeDasharray), blue color '#3b82f6'
    - EDGE_STYLES['timeout'] has dashed stroke (strokeDasharray: '8,4'), orange color '#f97316'
    - EDGE_STYLES['no_match'] has dashed stroke (strokeDasharray: '8,4'), gray color '#9ca3af'
    - EDGE_STYLES['intent'] has dotted stroke (strokeDasharray: '3,3'), red color '#ef4444'
    - Unknown edgeType falls back to 'next' style
    - getEdgeStyle('next') returns correct style object
    - getEdgeStyle('timeout') returns dashed orange style

    Note: Full component rendering tests for custom edges in jsdom are impractical because React Flow edge components require SVG context and internal ReactFlow state. Instead, export the EDGE_STYLES map and getEdgeStyle helper as testable units. Test the ConditionalEdge component structure via the exported constants.
  </behavior>
  <action>
    1. Create src/components/canvas/ConditionalEdge.tsx:
       - Export the EDGE_STYLES constant for testability:
         ```
         export const EDGE_STYLES: Record<string, { stroke: string; strokeDasharray?: string }> = {
           next:      { stroke: '#64748b' },                         // solid slate
           condition: { stroke: '#3b82f6' },                         // solid blue
           timeout:   { stroke: '#f97316', strokeDasharray: '8,4' }, // dashed orange
           no_match:  { stroke: '#9ca3af', strokeDasharray: '8,4' }, // dashed gray
           intent:    { stroke: '#ef4444', strokeDasharray: '3,3' }, // dotted red
         };
         ```
       - Export helper: `export function getEdgeStyle(edgeType: string)` that looks up EDGE_STYLES with fallback to 'next'
       - Export function ConditionalEdge(props: EdgeProps):
         - Destructure: sourceX, sourceY, targetX, targetY, sourcePosition, targetPosition, data, label, markerEnd, id
         - Compute path: `const [edgePath, labelX, labelY] = getSmoothStepPath({ sourceX, sourceY, sourcePosition, targetX, targetY, targetPosition })`
         - Get edgeType from data?.edgeType (cast to string) defaulting to 'next'
         - Get style from getEdgeStyle(edgeType)
         - Render BaseEdge with id, path={edgePath}, markerEnd, style={{ ...style, strokeWidth: 2 }}
         - If label exists, render EdgeLabelRenderer with positioned div:
           - style: position absolute, transform translate(-50%,-50%) translate(labelX, labelY), pointerEvents: 'all'
           - className: "nodrag nopan text-xs px-1.5 py-0.5 rounded shadow-sm border font-medium"
           - Add background color matching edge type: bg-white for next, bg-blue-50 border-blue-200 text-blue-700 for condition, bg-orange-50 border-orange-200 text-orange-700 for timeout, bg-gray-50 border-gray-200 text-gray-600 for no_match, bg-red-50 border-red-200 text-red-700 for intent
         - Export a LABEL_STYLES map for badge background/border colors per edge type

    2. Create src/components/canvas/edgeTypes.ts:
       - Module-level constant (NOT inside a component)
       - `export const edgeTypes: EdgeTypes = { conditional: ConditionalEdge }`

    3. Write tests in src/components/canvas/__tests__/ConditionalEdge.test.tsx:
       - Test EDGE_STYLES map entries for all 5 types
       - Test getEdgeStyle function returns correct styles including fallback
       - Test LABEL_STYLES map entries if exported
  </action>
  <verify>
    <automated>npx vitest run src/components/canvas/__tests__/ConditionalEdge.test.tsx --reporter verbose</automated>
  </verify>
  <done>
    ConditionalEdge component handles all 5 edge type styles.
    EDGE_STYLES map correctly defines solid/dashed/dotted with appropriate colors.
    Label badges styled per edge type.
    edgeTypes registry exports { conditional: ConditionalEdge } at module level.
    All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update edgeExtractors to emit type:'conditional' on all edges</name>
  <files>
    src/lib/edgeExtractors.ts,
    src/lib/__tests__/edgeExtractors.test.ts
  </files>
  <action>
    1. In src/lib/edgeExtractors.ts, add `type: 'conditional'` to every edge object:
       - The 'next' edge push (line ~28): add `type: 'conditional'`
       - The 'condition' edge push (line ~43): add `type: 'conditional'`
       - The 'timeout' edge push (line ~57): add `type: 'conditional'`
       - The 'no_match' edge push (line ~66): add `type: 'conditional'`
       - The 'intent' edge push (line ~82): add `type: 'conditional'`

    2. Update existing edge extractor tests to verify the `type: 'conditional'` field is present on all edges. The existing tests in src/lib/__tests__/edgeExtractors.test.ts likely assert on edge properties -- add type checks.

    3. Run full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>npx vitest run --reporter verbose</automated>
  </verify>
  <done>
    All edges emitted by extractEdgesFromStep include type: 'conditional'.
    Existing edge extractor tests updated and passing.
    Full test suite green (72+ existing + new ConditionalEdge tests).
  </done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter verbose` -- all tests pass
- EDGE_STYLES defines 5 distinct visual styles with correct colors and dash patterns
- ConditionalEdge reads data.edgeType and renders BaseEdge with matching style
- Labels render via EdgeLabelRenderer with type-appropriate badge colors
- edgeTypes.ts exports module-level { conditional: ConditionalEdge }
- All edges from edgeExtractors have type: 'conditional'
</verification>

<success_criteria>
- Next edges: solid slate (#64748b) lines
- Condition edges: solid blue (#3b82f6) lines with blue label badges
- Timeout edges: dashed (8,4) orange (#f97316) lines with orange badges
- No-match edges: dashed (8,4) gray (#9ca3af) lines with gray badges
- Intent edges: dotted (3,3) red (#ef4444) lines with red badges
- All labeled edges display their text in a styled badge
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/03-canvas-rendering-layout/03-02-SUMMARY.md`
</output>
